name: 📺 EPG Crawler (Boomerang)

on:
  schedule:
    - cron: "0 18 * * *"  # 01:00 Hà Nội (UTC+7)
  workflow_dispatch:      # Cho phép anh chạy thủ công

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          pip install requests beautifulsoup4 lxml

      - name: 🕸️ Crawl Boomerang EPG
        run: |
          python << 'EOF'
          import requests, datetime
          from bs4 import BeautifulSoup

          def fetch_epg(date_str):
              url = f"https://info.msky.vn/vn/Boomerang.html?date={date_str}"
              r = requests.get(url, timeout=20)
              r.encoding = "utf-8"
              soup = BeautifulSoup(r.text, "lxml")

              programmes = []
              for tr in soup.select("table tbody tr"):
                  tds = tr.find_all("td")
                  if len(tds) >= 2:
                      start, title = tds[0].get_text(strip=True), tds[1].get_text(strip=True)
                      programmes.append((start, title))
              return programmes

          today = datetime.date.today()
          nextday = today + datetime.timedelta(days=1)

          days = [today, nextday]
          xml = ['<?xml version="1.0" encoding="utf-8"?>',
                 '<tv generator-info-name="lps_crawler">']

          for d in days:
              date_str = d.strftime("%d/%m/%Y")
              progs = fetch_epg(date_str)
              for start, title in progs:
                  start_fmt = datetime.datetime.strptime(
                      f"{d.strftime('%Y-%m-%d')} {start}", "%Y-%m-%d %H:%M"
                  ).strftime("%Y%m%d%H%M%S +0700")
                  xml.append(f'<programme start="{start_fmt}" channel="Boomerang">')
                  xml.append(f'  <title lang="vi">{title}</title>')
                  xml.append('</programme>')

          xml.append('</tv>')
          with open("boomerang.xml", "w", encoding="utf-8") as f:
              f.write("\n".join(xml))

          print("✅ EPG file created successfully.")
          EOF

      - name: 💾 Commit and push XML
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git add boomerang.xml
          git commit -m "Update boomerang.xml: $(date '+%Y-%m-%d %H:%M:%S')" || echo "No changes to commit"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main

      - name: 🧾 Report summary
        shell: bash
        run: |
          COUNT_TODAY=$(grep -c "<programme" boomerang.xml || true)

          echo "## 🕒 EPG Crawl Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Date (Hanoi):** $(TZ='Asia/Ho_Chi_Minh' date '+%Y-%m-%d %H:%M:%S')" >> $GITHUB_STEP_SUMMARY
          echo "- **Total programmes parsed:** $COUNT_TODAY" >> $GITHUB_STEP_SUMMARY
          echo "- **Source:** [info.msky.vn](https://info.msky.vn/vn/Boomerang.html)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ **[EPG file updated successfully](https://github.com/${{ github.repository }}/blob/main/boomerang.xml)**" >> $GITHUB_STEP_SUMMARY
          
